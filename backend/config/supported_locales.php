<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Supported Locales
    |--------------------------------------------------------------------------
    |
    | The single list of locales FitnessLab supports. Both the backend
    | (Laravel lang/ files) and the frontend (react-i18next resources) are
    | expected to expose exactly these locales, so the two applications
    | cannot silently disagree about which languages exist.
    |
    */

    'supported' => ['en', 'pl'],

    'default' => 'en',

];
