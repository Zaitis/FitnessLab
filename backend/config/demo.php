<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Demo Account Credentials
    |--------------------------------------------------------------------------
    |
    | Not a secret — the whole point of the demo account is that these
    | credentials are published in the README so a reviewer can log in
    | directly, and the frontend's "Try the demo account" button submits
    | them itself. Fixed rather than env-overridable: the only two places
    | that need to agree on the value (this file and the frontend's demo
    | button) would otherwise be able to drift apart per-environment.
    |
    */

    'email' => 'demo@fitnesslab.zaitis.dev',

    'password' => 'FitnessLabDemo!2026',

];
